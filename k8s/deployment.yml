apiVersion: apps/v1
kind: Deployment
metadata:
  name: hlads-web
  namespace: hlads-web-ns
  labels:
    app: hlads-web
spec:
  strategy:
    rollingUpdate:
      maxSurge: 30%
      maxUnavailable: 0
    type: RollingUpdate
  replicas: 1
  selector:
    matchLabels:
      app: hlads-web
  template:
    metadata:
      labels:
        app: hlads-web
    spec:
      containers:
      - name: hlads-web
        image: hlads/website
        imagePullPolicy: Always
        resources:
          limits:
            memory: 350Mi
            cpu: "0.5"
          requests:
            memory: 300Mi
            cpu: "0.25"
        ports:
        - containerPort: 80